[project]
name = "pytorch-learning"
version = "0.1.0"
description = "My PyTorch learning notes and experiments"
readme = "README.md"
authors = [
    { name = "lyh", email = "lyh397408@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "torch>=2.14.0",
]

[tool.uv]
package = false
