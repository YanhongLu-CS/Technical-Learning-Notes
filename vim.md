| 场景          | 命令作用              | 例子                    |
| ----------- | ----------------- | --------------------- |
| 进入插入模式      | 在光标前开始输入          | `i`                   |
| 进入插入模式      | 在光标后开始输入          | `a`                   |
| 进入插入模式      | 在当前行开头输入          | `I`                   |
| 进入插入模式      | 在当前行末尾输入          | `A`                   |
| 新建一行        | 在当前行下方新建一行并进入插入模式 | `o`                   |
| 新建一行        | 在当前行上方新建一行并进入插入模式 | `O`                   |
| 退出插入模式      | 返回 Normal 模式      | `Esc`                 |
| 保存文件        | 保存当前文件            | `:w`                  |
| 退出 Vim      | 退出，前提是没有未保存修改     | `:q`                  |
| 保存并退出       | 保存文件后退出           | `:wq`                 |
| 保存并退出       | 等价于保存并退出          | `ZZ`                  |
| 强制退出        | 丢弃修改并退出           | `:q!`                 |
| 强制保存        | 忽略只读等限制尝试保存       | `:w!`                 |
| 保存为新文件      | 另存为               | `:w new.txt`          |
| 打开文件        | 在当前 Vim 中打开另一个文件  | `:e app.py`           |
| 左右移动        | 左 / 下 / 上 / 右     | `h` `j` `k` `l`       |
| 按单词移动       | 跳到下一个单词开头         | `w`                   |
| 按单词移动       | 跳到上一个单词开头         | `b`                   |
| 按单词移动       | 跳到当前/下一个单词结尾      | `e`                   |
| 跳到行首        | 跳到当前行第一个非空字符      | `^`                   |
| 跳到行首        | 跳到当前行绝对开头         | `0`                   |
| 跳到行尾        | 跳到当前行末尾           | `$`                   |
| 跳到文件开头      | 跳到第一行             | `gg`                  |
| 跳到文件结尾      | 跳到最后一行            | `G`                   |
| 跳到指定行       | 跳到第 N 行           | `100G` 或 `:100`       |
| 向下翻页        | 向下翻半页             | `Ctrl+d`              |
| 向上翻页        | 向上翻半页             | `Ctrl+u`              |
| 向下翻页        | 向下翻一页             | `Ctrl+f`              |
| 向上翻页        | 向上翻一页             | `Ctrl+b`              |
| 删除字符        | 删除光标所在字符          | `x`                   |
| 删除一行        | 删除当前行             | `dd`                  |
| 删除多行        | 删除 N 行            | `5dd`                 |
| 删除到行尾       | 删除光标到行尾内容         | `D` 或 `d$`            |
| 删除一个单词      | 删除到下一个单词开头        | `dw`                  |
| 删除当前单词      | 删除整个单词            | `diw`                 |
| 修改一个单词      | 删除当前单词并进入插入模式     | `ciw`                 |
| 修改到行尾       | 删除光标到行尾并进入插入模式    | `C`                   |
| 替换字符        | 替换光标所在的一个字符       | `ra` 把当前字符替换成 `a`     |
| 连续替换        | 进入 Replace 模式     | `R`                   |
| 复制一行        | 复制当前行             | `yy`                  |
| 复制多行        | 复制 N 行            | `5yy`                 |
| 复制一个单词      | 复制当前单词            | `yiw`                 |
| 粘贴          | 在光标后/下一行粘贴        | `p`                   |
| 粘贴          | 在光标前/上一行粘贴        | `P`                   |
| 撤销          | 撤销上一步操作           | `u`                   |
| 重做          | 恢复被撤销的操作          | `Ctrl+r`              |
| 搜索文本        | 从当前位置向下搜索         | `/error`              |
| 反向搜索        | 从当前位置向上搜索         | `?error`              |
| 下一个搜索结果     | 跳到下一个匹配           | `n`                   |
| 上一个搜索结果     | 跳到上一个匹配           | `N`                   |
| 搜索当前单词      | 向下搜索光标所在单词        | `*`                   |
| 搜索当前单词      | 向上搜索光标所在单词        | `#`                   |
| 替换当前行       | 当前行第一个匹配项替换       | `:s/foo/bar/`         |
| 替换当前行全部     | 当前行所有匹配项替换        | `:s/foo/bar/g`        |
| 全文件替换       | 全文件所有匹配项替换        | `:%s/foo/bar/g`       |
| 替换前确认       | 每次替换前询问           | `:%s/foo/bar/gc`      |
| 进入可视模式      | 按字符选择文本           | `v`                   |
| 进入行可视模式     | 按整行选择文本           | `V`                   |
| 进入块可视模式     | 按矩形块选择文本          | `Ctrl+v`              |
| 删除选中内容      | Visual 模式选择后删除    | `v` → 选择 → `d`        |
| 复制选中内容      | Visual 模式选择后复制    | `v` → 选择 → `y`        |
| 缩进          | 当前行增加缩进           | `>>`                  |
| 减少缩进        | 当前行减少缩进           | `<<`                  |
| 多行缩进        | Visual 模式选择后缩进    | `V` → 选择 → `>`        |
| 自动缩进        | 自动格式化当前行          | `==`                  |
| 自动缩进全文      | 自动格式化整个文件缩进       | `gg=G`                |
| 合并行         | 将下一行合并到当前行        | `J`                   |
| 跳转括号        | 在配对括号之间跳转         | `%`                   |
| 设置标记        | 给当前位置设置标记 `a`     | `ma`                  |
| 跳到标记        | 跳回标记 `a` 所在位置     | `'a`                  |
| 返回上次位置      | 在跳转位置之间切换         | `Ctrl+o`              |
| 前往下一位置      | 与 `Ctrl+o` 相反     | `Ctrl+i`              |
| 打开水平窗口      | 水平分屏              | `:split` 或 `:sp`      |
| 打开垂直窗口      | 垂直分屏              | `:vsplit` 或 `:vsp`    |
| 窗口切换        | 切到下一个窗口           | `Ctrl+w w`            |
| 窗口切换        | 左/下/上/右窗口         | `Ctrl+w h/j/k/l`      |
| 关闭窗口        | 关闭当前窗口            | `:close`              |
| 新建 Tab      | 新建标签页             | `:tabnew`             |
| Tab 切换      | 下一个标签页            | `gt`                  |
| Tab 切换      | 上一个标签页            | `gT`                  |
| 显示行号        | 开启行号              | `:set number`         |
| 关闭行号        | 关闭行号              | `:set nonumber`       |
| 相对行号        | 开启相对行号            | `:set relativenumber` |
| 高亮搜索        | 开启搜索结果高亮          | `:set hlsearch`       |
| 取消搜索高亮      | 临时清除高亮            | `:noh`                |
| 忽略大小写搜索     | 搜索时忽略大小写          | `:set ignorecase`     |
| 执行 Shell 命令 | 在 Vim 内执行系统命令     | `:!ls`                |
| 查看帮助        | 查看某命令帮助           | `:help dd`            |