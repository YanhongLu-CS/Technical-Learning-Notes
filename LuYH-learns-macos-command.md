| 场景           | 命令        | 作用                       | 例子                     |
| ------------ | --------- | ------------------------ | ---------------------- |
| 查看当前位置       | `pwd`     | 显示当前所在目录的完整路径            | `pwd`                  |
| 查看文件         | `ls`      | 列出当前目录中的文件和文件夹           | `ls -la`               |
| 切换目录         | `cd`      | 进入指定目录                   | `cd ~/Downloads`       |
| 创建文件夹        | `mkdir`   | 新建目录                     | `mkdir project`        |
| 创建文件         | `touch`   | 创建空文件，也可更新时间戳            | `touch notes.txt`      |
| 复制文件         | `cp`      | 复制文件或目录                  | `cp a.txt b.txt`       |
| 移动 / 重命名     | `mv`      | 移动文件，也可用于重命名             | `mv old.txt new.txt`   |
| 删除文件         | `rm`      | 删除文件或目录                  | `rm test.txt`          |
| 查看文件内容       | `cat`     | 在终端直接显示文件内容              | `cat README.md`        |
| 搜索文本         | `grep`    | 在文本或命令输出中查找关键词           | `grep "error" app.log` |
| 查找文件         | `find`    | 按文件名、路径等条件查找文件           | `find . -name "*.txt"` |
| 用 macOS 应用打开 | `open`    | 用 Finder 或默认应用打开文件、目录、网页 | `open .`               |
| 管理员权限执行      | `sudo`    | 以管理员权限运行命令               | `sudo mkdir /test`     |
| 查看帮助文档       | `man`     | 查看命令的官方手册                | `man ls`               |
| 查看历史命令       | `history` | 显示之前执行过的终端命令             | `history`              |


==`ls`和`ls -la`的区别：==

| 命令       | 含义       | 说明                        |
| -------- | -------- | ------------------------- |
| `ls`     | 列出目录内容   | 默认只显示普通可见文件/文件夹           |
| `ls -la` | 详细列出全部内容 | `-l` = 详细信息，`-a` = 包括隐藏文件 |

`ls -la`会看到：
```
权限        链接数  所有者  用户组  大小   修改时间        文件名
drwxr-xr-x   8     user   staff   256   Sep 5 10:30    .
```


==`rm`和`rm -rf`的区别：==

|命令|含义|风险|
|---|---|---|
|`rm file.txt`|删除单个文件|相对低|
|`rm -r folder`|递归删除文件夹及里面内容|高|
|`rm -rf folder`|强制、递归删除整个目录|很高|
